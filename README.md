# ID_site — UI Starter

Independent frontend-only copy of selected UI assets from z.ai_project.

Source repository is unchanged. The original project’s backend, infrastructure, databases, and credentials are not included.

> Frontend extraction is in progress; do not treat this initial commit as a complete runnable application.
